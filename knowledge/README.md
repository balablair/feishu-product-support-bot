# 知识库目录

将你的产品文档放在这里，bot 启动时会自动加载。

**支持的格式：**
- `.md` — Markdown 文档
- `.txt` — 纯文本
- `.pdf` — PDF 文件
- `.docx` — Word 文档
- `.pptx` — PowerPoint 演示文稿

**提示：**
- 支持子目录，会递归扫描所有文件
- 也可以通过 `FEISHU_DOC_TOKENS` 配置飞书云文档，无需下载到本地
- 建议文件名清晰，便于 bot 在回答时引用正确内容
